{
  "name": "iamemmanouil-website",
  "description": "Repository containing the source for my website.",
  "version": "0.1.0",
  "scripts": {
    "build-less": "lessc --silent _less/_base.less static/css/base.css",
    "watch": "watch 'npm run build-less' _less/",
    "start": "npm run watch & jekyll serve --watch"
  },
  "devDependencies": {
    "less": "^2.5.1",
    "watch": "^0.16.0"
  }
}
